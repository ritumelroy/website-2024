export interface ClassCardProps {
  status: string;
  title: string;
  year: string;
  term: string;
  description: string;
  badges: any;
}

export interface CurrProjProps {
  image: string;
  title: string;
  desc: string;
  status: string;
  percentage: number;
  deadline: string;
  github: string;
  website: string;
  badges: string[];
}

// images, title, description, status, percentage, deadline, github, website, badges,
